export const metadata = {
    title: "Privacy Policy | Jack's Software House"
}

export default function Layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
