export const metadata = {
    title: "Pricing & Payments | Jack's Software House"
}

export default function Layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
