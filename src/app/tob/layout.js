export const metadata = {
    title: "Terms of Business | Jack's Software House"
}

export default function Layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
