export const metadata = {
    title: "Terms & Conditions | Jack's Software House"
}

export default function Layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
